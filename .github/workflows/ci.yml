name: Cypress Tests

on: push

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      uses: cypress-io/github-action@v6
  with:
    build: npm run build
    start: npm start
    browser: chrome